### @hideIteration true
### @flyoutOnly false
### @explicitHints true
### @hideDone true

# 2-2

## 

```template
player.onChat("go", function () {
})
```

```ghost
player.onChat("go", function () {
    mobs.spawn(CHICKEN, posCamera(0, 0, 0))
    blocks.place(GRASS, pos(0, 0, 0))
})
```
